/** Chill, simple product copy + INR pricing (shared across shell + meetings). */

export const PORTIQ_PRICE_ROW =
  'Starter ₹1,499 · Pro ₹2,999 · Business ₹9,999 / mo · Institutional (contact sales)';

/** One-liner for boot / meta — includes tasks, not just transcripts. */
export const PORTIQ_TAGLINE =
  'Transcripts, summaries, and tasks from every meeting—without the busywork.';

/** Under the new-meeting form — reminder that tasks are part of the product. */
export const PORTIQ_MEETINGS_HINT =
  'Every meeting gets a transcript, a tight summary, and a task list you can run with.';
