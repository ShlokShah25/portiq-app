# Voice enrollment + speaker match (CPU wheels — used on Railway / Linux hosts).
# Installed during Nixpacks build; see nixpacks.toml
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0,<2.6
torchaudio>=2.1.0,<2.6
numpy>=1.24.0,<3
pyannote.audio>=3.1.0,<4.0
huggingface_hub>=0.20.0
soundfile>=0.12.0
